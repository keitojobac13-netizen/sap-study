/**
 * The generated card at `app/opengraph-image.tsx`.
 *
 * It is referenced explicitly rather than left to the file convention: any
 * page that declares its own `openGraph` block replaces the inherited one
 * wholesale, so the convention's image silently disappears from every module
 * and section page. Spelling it out keeps the card on all 195 URLs.
 */
export const OG_IMAGE = {
  url: '/opengraph-image',
  width: 1200,
  height: 630,
  alt: 'SAP学習ポータル / SAP Study Portal',
} as const;

export const OG_IMAGES = [OG_IMAGE];
